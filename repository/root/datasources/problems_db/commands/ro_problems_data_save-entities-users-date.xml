<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE command SYSTEM "../../../../dtds/script-dao-commands.dtd">
<command name="ro.problems.data.save-entities-users-date" type="SQL.UPDATE">
         <parameters>
                     <parameter name="entityId" />
                     <parameter name="userId" />
                     <parameter name="vote" />
                     <parameter name="priority" />
                     <parameter name="statusId" />
                     <parameter name="flag" />
               </parameters>
      <script>
      update entities_users
#if($flag=="vote" )
#if($vote)
set vote='$vote'
#else
set vote=null
#end
#elseif($flag=="priority")
#if($priority )
set priority=$priority
#else
set priority=null
#end
#elseif($flag=="status")
#if($statusId != -1)
set status_id=$statusId
#else
set status_id=null
#end
#end
where entity_id=$entityId and user_id=$userId

   </script>
</command>